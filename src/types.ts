export type Role="user"|"admin";export type OrderStatus="Pending"|"Confirmed"|"Preparing"|"Ready"|"Completed"|"Cancelled";export type ReservationStatus="Pending"|"Confirmed"|"Cancelled"|"Completed";export interface MenuItem{id:string;name:string;description:string;price:number;categoryId:string;imageUrl:string;ingredients?:string[];allergens?:string[];isVeg:boolean;isPopular:boolean;isAvailable:boolean}export interface CartItem extends MenuItem{quantity:number}